{
  "type": "schema",
  "name": "column_model",
  "description": "Column schema for Kanban board - IMPLEMENTED BY AI #2",
  "version": "1.0.0",
  "last_updated": "2025-11-24",
  "status": "implemented",
  "file": "src/models/Column.js",
  "implemented_by": "AI #2 using CDD context files",
  
  "schema": {
    "name": {
      "type": "String",
      "required": true,
      "minlength": 2,
      "maxlength": 50,
      "description": "Column name (e.g., 'To Do', 'In Progress', 'Done')"
    },
    "boardId": {
      "type": "ObjectId",
      "ref": "Board",
      "required": true,
      "description": "Parent board reference"
    },
    "position": {
      "type": "Number",
      "default": 0,
      "description": "Order within board (auto-incremented)"
    },
    "createdAt": {
      "type": "Date",
      "auto": true,
      "description": "Mongoose timestamp"
    },
    "updatedAt": {
      "type": "Date",
      "auto": true,
      "description": "Mongoose timestamp"
    }
  },
  
  "relationships": {
    "belongs_to": "Board",
    "has_many": "Task",
    "cascade_delete": "Deleting column removes all tasks in that column"
  },
  
  "indexes": [
    {"fields": ["boardId", "position"], "purpose": "Sort columns within board efficiently"}
  ],
  
  "hooks": {
    "pre_save": "Auto-increments position for new columns on the same board"
  },
  
  "validation_rules": [
    "Name: 2-50 characters, required",
    "boardId: required ObjectId reference to Board"
  ],
  
  "api_endpoints": [
    "GET /api/columns?boardId=xxx - List columns for a board",
    "GET /api/columns/:id - Get single column",
    "POST /api/columns - Create column",
    "PUT /api/columns/:id - Update column",
    "DELETE /api/columns/:id - Delete column (cascade to tasks)",
    "PATCH /api/columns/reorder - Batch update positions for drag-and-drop"
  ],
  
  "cdd_notes": {
    "pattern_reuse": "Copied position auto-increment pattern from Task model pre-save hook",
    "learned_from_context": "Followed established patterns in task_model.json and task_crud.json"
  }
}
